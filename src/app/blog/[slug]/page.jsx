export function generateMetadata({ params }) {
  return {
    title: `Blog Post | Vyranza`,
    description: "Read this article on Vyranza's blog.",
  };
}

export default function BlogPostPage({ params }) {
  return null;
}
